# Phase 2 (baseline + playback manifest mode)
sounddevice>=0.4.6
numpy>=1.24.0

# Dashboard server (Phase 2.5)
fastapi>=0.110
uvicorn[standard]>=0.27

# Snippet capture (--save-snippets): WAV writer + spectrogram renderer
soundfile>=0.12.1
matplotlib>=3.7.0

# Phase 0: PDF literature verification (can be removed after use)
PyPDF2>=3.0.0
