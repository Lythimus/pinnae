# Phase 2 (baseline + playback manifest mode)
sounddevice>=0.4.6
numpy>=1.24.0

# Dashboard server (Phase 2.5)
fastapi>=0.110
uvicorn[standard]>=0.27

# Audio clip recording (FLAC writer) + Phase 3 duplex playback
soundfile>=0.12.1

# Video clip recording (optional, requires --video; any UVC camera via OpenCV)
opencv-python>=4.8

# Phase 0: PDF literature verification (can be removed after use)
PyPDF2>=3.0.0
